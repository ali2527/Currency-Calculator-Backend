{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/currencies", "dest": "/api/currencies.ts" },
    { "src": "/convert", "dest": "/api/convert.ts" }
  ]
}